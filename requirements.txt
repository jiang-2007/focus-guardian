# Focus Guardian 依赖清单
# 安装方法：pip install -r requirements.txt

psutil>=5.9.0         # 进程检测，获取系统进程信息
pywin32>=306          # Windows API：窗口检测、注册表操作
pystray>=0.19.0       # 系统托盘图标
Pillow>=10.0.0        # 图片处理（生成托盘图标）
